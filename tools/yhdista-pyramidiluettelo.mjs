// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

#// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

H// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

D// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

R// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

M// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

D// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

U// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

+// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

→// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

K// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

M// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

K// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

—// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

6// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

9// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

2// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

0// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

2// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

6// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

"// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

+// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

1// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

0// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

4// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

"// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ö// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

M// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

R// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

H// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

U// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

U// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

K// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

5// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

–// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

7// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

5// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

–// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

7// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

—// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ö// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

0// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

—// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

L// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

5// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

–// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

K// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ö// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

M// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

\// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

\// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

\// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

3// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

\// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

"// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

3// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

M// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

R// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

"// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

\// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

"// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

A// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

"// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

\// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

x// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

3// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

0// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

0// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

*// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

F// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

F// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

2// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

|// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

|// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

|// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

|// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

K// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ö// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

<// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

<// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

<// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

x// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

2// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

F// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

F// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

—// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

C// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

!// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

×// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

V// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

·// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

8// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

—// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

/// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

&// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

&// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

>// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

-// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

[// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

]// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

=// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

·// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

E// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

T// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

Ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

I// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

m// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

p// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

—// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

5// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

–// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

z// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

7// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

w// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

F// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

J// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

S// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

N// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

f// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

2// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

·// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

d// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

P// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

·// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

ä// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

'// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

·// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

:// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

,// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

v// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

r// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

i// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

$// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

O// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

b// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

j// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

c// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

k// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

y// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

(// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

u// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

?// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

{// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

.// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

l// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

e// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

g// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

h// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

}// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

 // Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

n// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

s// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

t// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

o// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

a// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

`// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

)// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

;// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}


// Nostotaso: z8 mukaan vanhalla nostot-taululla (perustelu yllä).
const ilmanNostoja = process.argv.includes('--ilman-nostoja');
const vanhatNostot = Object.keys(vanha.nostotaso?.nostot ?? {}).length;
const uudetNostot = Object.keys(uusi.nostotaso?.nostot ?? {}).length;
if (uusi.nostotaso?.laatastot?.[Z] && !ilmanNostoja) {
  tulos.nostotaso.tasot = [...new Set([...(vanha.nostotaso.tasot ?? []), Z])].sort((a, b) => a - b);
  tulos.nostotaso.laatastot ??= {};
  tulos.nostotaso.laatastot[Z] = uusi.nostotaso.laatastot[Z];
  lisatty.push(`nostotaso z${Z}`);
  if (vanhatNostot !== uudetNostot) {
    console.warn(`· nostotaso z${Z} on poltettu ${uudetNostot} nostolla, taulu pidetaan vanhana (${vanhatNostot}) — `
      + 'uudet nostot piirtyvat z8:lla seka poltettuina etta elavina; siisti tila vaatii nostotason uusintapolton');
  }
} else {
  console.warn(`· nostotaso z${Z} jätetään pois (${ilmanNostoja ? '--ilman-nostoja' : 'ei laatastoa'})`);
}

